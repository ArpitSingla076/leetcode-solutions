 +++ Two Concept Used +++
Substring: "substring(0, i+1)" gives the part of the string from the start up to the last odd digit(where start is included and last is excluded).
ch - '0': "Subtracting '0' from a digit character converts it to its actual integer value."


class Solution {
    public String largestOddNumber(String num) {
        int n = num.length();
        for(int i=n-1;i>=0;i--) {
            char ch = num.charAt(i);
            if((ch-'0') %2 !=0) {
                return num.substring(0,i+1);
            }
        }
        return "";
    }
}



--- using start and end (two pointer approach) ---
class Solution {
    public String largestOddNumber(String num) {
        int n = num.length();
        int end = n-1;
        while(end>=0) {
            char ch = num.charAt(end);
            if((ch-'0') %2 !=0) {
                return num.substring(0,end+1);
            }
            end--;
        }
        return "";
    }
}
